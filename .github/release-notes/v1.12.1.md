# Komari-Theme-LuminaLite v1.12.1

## 修复

1. **修复未登录时小视图（紧凑卡）名称下方出现空白行**。紧凑卡的 chip 行固定 24px 高度且无论有无内容都会渲染，后端不向访客下发 `ipv4`/`ipv6` 字段，节点若无副标题、标签则该行内容为空，在名称下方留出一行空白。现与迷你卡一致，在无副标题、无 IP 徽章、无标签时整行不渲染，空白消除。
