# Komari-Theme-LuminaLite v1.12.2

## 新增

1. **首页标题旁显示主题版本号**。通过构建时注入 `komari-theme.json` 的版本号，站点铭牌始终展示当前主题版本（hover 可查看后端版本）。

## 修复

1. **修复离线节点仍显示为在线的问题**。后端 `online` 列表可能存在延迟（节点已断开但尚未移除），前端新增数据新鲜度校验：若节点声称在线但 `updated_at` 距今超过 30 秒，主动修正为离线状态。
