# Komari-Theme-LuminaLite v1.12.7

## 新功能

1. **首页概览卡片悬停显示 TOP 3 节点**。鼠标移至「实时带宽」「累计流量」「实时连接」卡片时，浮层展示该指标最高的前 3 个节点及其数值，快速掌握各节点贡献分布。

## 优化

1. **实例详情页有 GPU 型号但无 GPU 数据上报时隐藏图表**。此前只要节点标注了 GPU 型号（`gpu_name`），即使后端从未上报 GPU 使用率、显存、温度等指标，GPU 图表也会显示为空。现在仅在确能获取到 GPU 数据时才渲染相关图表。
