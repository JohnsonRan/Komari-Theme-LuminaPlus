# Komari-Theme-LuminaLite v1.11.2

## 修复

1. **修复 GPU 实时数据不显示**。后端 `common:getNodesLatestStatus` RPC 硬编码 `gpu: 0`，该路径永远无法获取 GPU 数据。现改为与默认主题相同的 WebSocket 通道（`/api/clients`）获取完整 `v1.Report`（含 GPU 占用率、显存、温度），RPC 轮询仅在 WebSocket 不可用时作为降级兜底。
